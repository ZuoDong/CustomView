<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dong.customviews">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".simpleviewpager.SimpleVPActivity"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".xformode.XforModeActivity" />
        <activity android:name=".redpoint.RedPointActivity" />
        <activity android:name=".flowlayout.FlowLayoutActivity" />
        <activity android:name=".tickview.TickActivity" />
        <activity android:name=".polygonimageview.PolygonActivity" />
        <activity android:name=".LeafLoading.LeafLoadingActivity" />
        <activity android:name=".pathmeasure.PathMeasureActivity" />
        <activity android:name=".foldlayout.FoldLayoutActivity" />
        <activity android:name=".cameratansform.CameraTransActivity" />
        <activity android:name=".drawtext.DrawTextActivity" />
    </application>

</manifest>